package com.landolisp

import android.app.Application

/**
 * Android [Application] entry point.
 *
 * Kept intentionally empty for the skeleton — DI / global state can hook in later.
 * Registered in `AndroidManifest.xml` via `android:name=".LandolispApplication"`.
 */
class LandolispApplication : Application()
